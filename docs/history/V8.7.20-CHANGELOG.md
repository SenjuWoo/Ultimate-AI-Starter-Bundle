# v8.7.20 - Reviewed Hermes configuration and profile sync

**167 canonical skills**; no new dependency or always-on MCP server.

The portable starter incorporates the user's DeepSeek V4.1 Flash/high choice,
20% compression target, free Inkling Small summary model and 600-second timeout,
bounded delegation and disabled response caching. One-hour prompt caching stays
enabled. The 160k cap, 80k material-result pruning and summary-failure abort remain.
Inkling uses its supported high effort rather than the exported ultra setting.
Model/effort compatibility was checked against the public OpenRouter catalog on
September 15, 2026; no paid inference or summary-quality benchmark was run.

Existing settings are preserved, including profile-specific memory sizes.
Fresh users get update backups enabled and no private local-model aliases,
command approvals, hooks, absolute MCP commands or generated tool inventories.
Four profile descriptions and the shared-config policy replace redundant private
config copies. RimWorldForge remains external: only an already configured
rimworld profile is adopted for core updates; its server and filters stay intact.

The missing-only repair settings now match the starter's cap, target and response
cache choice. Profile migration updates stale Context7 package pins from CATALOG,
retaining other arguments and custom launchers. Windows PowerShell tests execute
that actual replacement block, including idempotence and unrecognized wrappers.
Existing hook, preservation and portability gates remain required.

v8.7.19 remains unchanged; this patch preserves its fixes and release history.
